#include <iostream>
using namespace std;

extern const int a; // Для задания №4
extern const int b; // Для задания №4
extern const int c; // Для задания №4
extern const int d; // Для задания №4

int main()
{
    // Задание №1 и 4 ======================================================================================================================================================================================================================================================
    float fF; 
    fF = static_cast<float> (a) * (b + (c / d));
    cout << fF << endl << endl;
    // Задание №1 и 4 ======================================================================================================================================================================================================================================================

    // Задание №2 ==========================================================================================================================================================================================================================================================
    int iTern, cond;
    cout << "Input integer nomber: ";
    cin >> cond;
    iTern = (cond <= 21) ? (21 - cond) : (2 * (cond - 21));
    cout << iTern << endl << endl;
    // Задание №2 ==========================================================================================================================================================================================================================================================

    // Задание №3 ==========================================================================================================================================================================================================================================================
    int Array[3][3][3], * pArr;
    int Sum = 0;
    for (int i = 0; i < 3; i++)
    {
        for (int j = 0; j < 3; j++)
        {
            for (int k = 0; k < 3; k++)
            {
                Array[i][j][k] = ++Sum;
                //cout << Array[i][j][k] << ' ';
                //if (k == 2)
                //{
                //    cout << endl;
                //}
            }
        }   
    }
    pArr = &Array[1][1][1];
    cout << "In array's center located nomber - " << *pArr << endl;
    // Задание №3 ==========================================================================================================================================================================================================================================================

    return 0;
}

